<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MobileApp1.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25"
            VerticalOptions="Center">
            
            <Entry 
                x:Name="txtMail"    
                Placeholder="Email"/>
            
            <Entry 
                x:Name="txtPass"
                Placeholder="Senha"
                IsPassword="True"/>
            
            <Button
                x:Name="btn1"
                Text="Entrar" 
                SemanticProperties.Hint="button acces app"
                Clicked="btn1_Clicked"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
